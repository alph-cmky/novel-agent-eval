# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Downloaded external datasets (not committed)
# brief 写的是 dataset/external/，但中间斜杠的 pattern 锚定仓库根目录，
# 匹配不到实际路径 novel_agent_eval/dataset/external/；用 **/ 覆盖任意层级。
**/dataset/external/

# Local evaluation traces and API outputs (publish selected summaries explicitly)
traces/
